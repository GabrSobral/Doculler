import { Sidebar } from "phosphor-react"; 

interface Props {

}

export const Header = ({ } : Props) => {
  return (
    <header className="w-full bg-light-primary-300 h-16 flex items-center sticky top px-4">

    </header>
  )
}